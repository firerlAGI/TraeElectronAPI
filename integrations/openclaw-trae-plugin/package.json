{
  "name": "trae-ide",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw plugin that delegates IDE tasks to a local TraeAPI instance.",
  "engines": {
    "node": ">=22"
  },
  "openclaw": {
    "extensions": [
      "./index.js"
    ]
  }
}
